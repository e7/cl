脚本功能：1. 为一个指定的目录建立镜像目录树，而文件则以软链接形式存在,
          但不包含隐藏文件或目录，这些文件通常有特殊用途。
注意事项：1. 鉴于realpath非系统核心包，所以使用cl脚本时务必确保系统中
          安装了realpath，如果没有安装可以将此realpath.c编译后使用，
          确保PATH路径中能找到realpath。
          2. 现在只能镜像git仓库，如果需要镜像普通目录，需简单修改cl脚本。
